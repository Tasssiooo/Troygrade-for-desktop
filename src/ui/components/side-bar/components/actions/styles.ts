import { css } from "../../../../../../styled-system/css";

const actions_wrapper = css({
  display: "flex",
  flexDir: "row",
  justifyContent: "space-between",
  w: "full",
  h: "8",
  pl: "1.5",
});

const buttons_wrapper = css({
  display: "flex",
});

export { actions_wrapper, buttons_wrapper };
