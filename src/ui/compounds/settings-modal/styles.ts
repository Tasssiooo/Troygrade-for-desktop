import { css } from "~/styled-system/css";

const content = css({
  display: "flex",
  flexDir: "row",
});

const separator = css({
  w: "1px",
  h: "200px",
  mx: "4",
  bg: "#1f292e",
});

export { content, separator };
