import { css } from "~/styled-system/css";

const container = css({
  minW: "320px",
  maxW: "420px",
});

const header = css({
  px: "1.5",
  bg: "#614920",
});

const info = css({
  p: "1.5",
  fontSize: "sm",
});

export { container, header, info };
